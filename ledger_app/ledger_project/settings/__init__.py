# Settings are split by environment. The active module is selected via the
# DJANGO_SETTINGS_MODULE environment variable (set in .env or the shell).
#
# Typical values:
#   ledger_project.settings.local       – LOCAL / developer laptop
#   ledger_project.settings.production  – STG / PROD servers
